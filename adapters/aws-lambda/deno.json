{
  "compilerOptions": {
    "strict": true
  },
  "fmt": {
    "lineWidth": 88
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  },
  "tasks": {
    "check": "deno fmt --check && deno lint && deno check lambda/handler.ts test/http_api_v2_test.ts && deno test --no-check test"
  }
}
