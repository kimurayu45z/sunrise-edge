{
  "compilerOptions": {
    "strict": true
  },
  "fmt": {
    "lineWidth": 88
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  },
  "tasks": {
    "check": "deno fmt --check && deno lint && deno check supabase/functions/sunrise-edge/index.ts test/adapter_test.ts && deno test --no-check test"
  }
}
