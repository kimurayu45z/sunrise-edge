[package]
name = "contract-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rust SDK for Sunrise Edge WASM smart contracts"

[lib]
crate-type = ["rlib"]

[dependencies]

[dev-dependencies]
